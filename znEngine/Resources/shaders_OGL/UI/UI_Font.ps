#version 440
#extension GL_ARB_explicit_uniform_location : enable

// Input
in struct
{
	vec4 POSITION;
	vec2 TEXCOORD0;
} VSInput;

// Uniforms
uniform Material 
{
    vec4 Color;
	vec2 Offset;
};

layout(location = 0) uniform sampler2D DiffuseTexture;

// Output
layout(location = 0) out vec4 color;

void main()
{
	color = vec4(1.0f, 1.0f, 1.0f, 1.0f);//vec4(texture(DiffuseTexture, VSInput.TEXCOORD0).rgb, 1.0f);
};
